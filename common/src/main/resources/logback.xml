<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <!-- Console appender -->
  <appender class="ch.qos.logback.core.ConsoleAppender" name="CONSOLE">
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>
  <!-- File appender -->
  <appender class="ch.qos.logback.core.rolling.RollingFileAppender" name="FILE">
    <file>logs/kishax-api.log</file>
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <fileNamePattern>logs/kishax-api.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
      <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
        <maxFileSize>100MB</maxFileSize>
      </timeBasedFileNamingAndTriggeringPolicy>
      <maxHistory>30</maxHistory>
      <totalSizeCap>3GB</totalSizeCap>
    </rollingPolicy>
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>
  <!-- Logger for Kishax API -->
  <logger level="${LOG_LEVEL:-INFO}" name="net.kishax.api"></logger>
  <!-- Logger for AWS SDK -->
  <logger level="${AWS_SDK_LOG_LEVEL:-WARN}" name="software.amazon.awssdk"></logger>
  <!-- Logger for Lettuce (Redis) -->
  <logger level="${LETTUCE_LOG_LEVEL:-WARN}" name="io.lettuce"></logger>
  <!-- Logger for Apache HTTP Client -->
  <logger level="WARN" name="org.apache.hc"></logger>
  <!-- Root logger -->
  <root level="INFO">
    <appender-ref ref="CONSOLE"></appender-ref>
    <appender-ref ref="FILE"></appender-ref>
  </root>
</configuration>
